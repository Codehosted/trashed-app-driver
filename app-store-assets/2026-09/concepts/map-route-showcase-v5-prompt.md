# Driver marker and open context window — v5

Mode: built-in image_gen. App-store artwork only; no app UI changes.

Use case: precise-object-edit.
Asset type: Trashed app-store marketing artwork, not a literal screenshot or a live-data claim.
Input image 1 is the edit target: the current map showcase with a BLACK "trashed" wordmark and a purple symbol.
Input image 2 is a supporting existing app reference for the circular driver map marker ONLY. Do not reproduce its website header, demo page, speed labels, or map viewport.

Add one distinct DRIVER location marker on the purple route between stop 1 and stop 2, with its anchored context/chat window visibly OPEN. This is a driver avatar marker, NOT a fourth numbered stop and NOT a truck illustration.
Driver marker: a prominent circular avatar with "DD" initials, a crisp purple ring, white separation from the map, and a small green online dot. Use a neutral demo identity, no photograph or real person's information. Place it on the route near the middle-left bend, approximately x365/y1020 in the 851x1848 composition.
Context window: a clean white map popup above this driver marker with a small pointer/tail connecting directly to it. Make it deliberately large and readable for app-store artwork, about x55–460/y675–970, but keep the headline, the three numbered route stops, and the large bottom route card unobstructed. Use one white surface, restrained rounded corners and soft shadow, without nested purple mats, a thick frame, or card-within-card padding. This popup is the real app's DRIVER CHAT context, faithfully based on the current DriverChatPopup component; do not invent route or call controls.
Exact popup content:
- Header: "Demo Driver" in bold dark text, small × close icon at the upper right.
- Secondary line with green dot: "online · 1 route assigned"
- Airy empty conversation area: "No messages yet."
- Bottom composer: paperclip icon, "Message driver…" placeholder, and a subdued disabled send arrow. A thin divider/underline is enough; avoid a heavy nested input panel.
No address, speed, live timestamp, fabricated message, customer, phone number, delivery, order, or extra buttons.

Lock every other element: "trashed" stays BLACK, the brand symbol stays purple, neither has a background tile. Preserve the corrected single-storey "a". Preserve exact headline "Your business." / "Your routes." / "One app." with existing colors and positions. Preserve existing full-bleed pale city map, all three numbered stop markers, purple route geometry, bottom white route card, exact text "ROUTE 1", "0/3 stops", "8:00 AM · in progress", original typography, scale, shadows, portrait aspect ratio and crop. Only add the driver marker and its connected popup. No device frame, status bar, outer padding, gradient panels, new tagline, or redesign.
